const config = {
  // The name of the application
  REACT_APP_NAME: "Alpha Trades",
  // Supported Chains
  SUPPORTED_CHAID_IDS: [56, 97],

  // Infura ID
  REACT_APP_INFURA_ID: "",
};

export default config;
